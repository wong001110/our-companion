export { detectContext, isContextExpired } from './context-detector';
export type { ContextCategory, ContextState, ContextSignal, ContextDetectorInput } from './types';
export { CONTEXT_PRIORITY, CONTEXT_STABILIZATION_MS, CONTEXT_EXPIRY_MS } from './types';
