/** Must match `name` in apps/desktop/package.json for Electron userData. */
export declare const ELECTRON_APP_NAME = "@our-companion/desktop";
export declare function getDefaultUserDataRoot(): string;
