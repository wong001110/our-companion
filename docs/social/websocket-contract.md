# Companion invalidation

`companion.profile.updated`, `companion.profile.unpublished`, and `companion.asset_pack.activated` are cache invalidation hints. The client re-fetches REST data and verifies any Asset Pack before activating its cache.
