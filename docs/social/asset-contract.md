# Asset Pack V1

Electron Main recursively validates managed assets, rejects links and unsafe names, hashes allowed files, emits explicit mappings for `Idle_Neutral`, `Enter`, and `Leave`, canonicalizes the manifest, and computes SHA-256. Voices are excluded unless the owner opts in.
