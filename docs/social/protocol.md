# Protocol 0.3

The client sends protocol `0.3` and version `0.3.0`. It enables S4 Visit invitations and sessions through authenticated Main-process requests, keeps pre-signed asset URLs in the main process, and respects server storage capability failures. `visualVisits` remains disabled.
