# Desktop privacy boundary

SQLite mappings are scoped by server origin and network account. Managed cache roots are hashed by server origin and remain Main-process-only. Partial downloads are verified and atomically renamed; unpublish blocks new authorization but cannot erase a previously verified local cache.
