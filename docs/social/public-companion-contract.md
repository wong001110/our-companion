# Public Companion profile

The desktop links a local Companion to a different server-generated Network Companion in local SQLite, scoped by server origin and network account. Only approved public profile fields are sent; the local ID stays in Electron Main and never crosses the network.
