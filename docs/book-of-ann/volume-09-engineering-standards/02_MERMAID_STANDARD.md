# Mermaid Standard

Every engine must provide:

- flowchart
- sequenceDiagram
- stateDiagram-v2
- classDiagram
- erDiagram (where appropriate)

Diagrams become the canonical reference for implementation.
